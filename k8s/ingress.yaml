apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: image-gallery-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: image-gallery-service
            port:
              number: 80